{
  "name": "avatar",
  "version": "0.1.0",
  "description": "Agent identity creator — /avatar opens the Mini App wizard",
  "entry": "./index.ts",
  "env": {
    "AVATAR_APP_URL": "HTTPS URL where agent-avatar is deployed (required)"
  },
  "commands": [
    {
      "command": "avatar",
      "description": "配置角色身份"
    }
  ]
}
